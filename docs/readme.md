### how to check db from console
1. install cli via the following command: `sudo apt install sqlite3`
2. open database. E.g.: `open /home/ubuntu/tbot/lonesomejan.db`
3. select data. E.g.: `SELECT * FROM user;`
